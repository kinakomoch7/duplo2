import { IconButtonProps, TypographyProps } from "@mui/material";
import { ReactNode } from "react";

export type NIconButtonProps = {
  ButtonProps?: IconButtonProps,
  Icon?: ReactNode,
  TypoProps?: TypographyProps
}